# Auto detect text files and perform LF normalization
* text=auto

# Shell scripts, Docker and compose files must use LF to run inside containers.
*.sh text eol=lf
Dockerfile text eol=lf
*.yml text eol=lf
*.yaml text eol=lf

